name: pyramid_lint_example
description: Example of using pyramid_lint.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  custom_lint: ^0.6.2
  pyramid_lint:
    path: ../
